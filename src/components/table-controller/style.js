import styled from "styled-components";

export const ConentWrapper = styled.div`
  text-align: right;
  padding: 0 24px 12px;
  .anticon {
    cursor: pointer;
    margin-left: 10px;
  }
  .column-settings {
    display: inline-block;
    position: relative;
  }
`;
